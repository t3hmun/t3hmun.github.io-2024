---
const props = Astro.props;
---

<h4
    {...props}
    class="mx-auto mt-8 mb-4 max-w-3xl text-2xl font-semibold dark:text-amber-200"
>
    <a
        href={"#" + props.id}
        class="after:ml-2 after:text-stone-500 hover:after:content-['#']"
        ><slot /></a
    >
</h4>
